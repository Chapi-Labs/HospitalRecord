{% extends 'base.html.twig' %}
      
    {% block title  %}An Error Occurred: {{ status_text }}{% endblock %}
  
    {% block body %}
        <div style="text-align:center">
            <h1>A ocurrido un error</h1>
            <h2>El servidor retorno: "{{ status_code }} {{ status_text }}".</h2>
            <img src="{{ path('images/error-500.jpg')}}" />
              <p>Ocurrió un error interno, lamentamos los incovenientes, estamos trabajando para arreglarlo.</p>
        </div>
        <a class= "btn btn-default btn-lg btn-warning"  href="{{ path('homepage') }}">
            Regresar
        </a>
{% endblock %}
