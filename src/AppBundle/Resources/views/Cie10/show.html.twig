{% extends '::base.html.twig' %}

{% block body -%}
         <div class="row">
    <div class="col-md-1"></div>
    <div class="col-md-10">
    <h1>Cie10</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Codigo</th>
                <td>{{ entity.codigo }}</td>
            </tr>
            <tr>
                <th>Diagnostico</th>
                <td>{{ entity.diagnostico }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('cie10') }}">
            Regresar
        </a>
    </li>
    <li>
        <a class="btn btn-success" href="{{ path('cie10_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
 </div>
    <div class="col-md-1"></div>
</div>
{% endblock %}
