{% extends '::base.html.twig' %}

{% block body -%}
    <div class="row">
    <div class="col-md-1"></div>
    <div class="col-md-10">
    <h1>IngresoPaciente</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Fechaingreso</th>
                <td>{{ entity.fechaIngreso|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Motivoingreso</th>
                <td>{{ entity.motivoIngreso }}</td>
            </tr>
            <tr>
                <th>Procedimientorealizado</th>
                <td>{{ entity.procedimientoRealizado }}</td>
            </tr>
            <tr>
                <th>Diagnostico1</th>
                <td>{{ entity.diagnostico1 }}</td>
            </tr>
            <tr>
                <th>Diagnostico2</th>
                <td>{{ entity.diagnostico2 }}</td>
            </tr>
            <tr>
                <th>Diagnostico3</th>
                <td>{{ entity.diagnostico3 }}</td>
            </tr>
            <tr>
                <th>Fechasalida</th>
                <td>{{ entity.fechaSalida|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ entity.slug }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ entity.created|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ entity.updated|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Contentchanged</th>
                <td>{{ entity.contentChanged|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ingresopaciente') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ingresopaciente_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
    </div>
    <div class="col-md-1"></div>
    </div>
{% endblock %}
